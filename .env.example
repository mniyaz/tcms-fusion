# Docs authentication (used by Netlify Functions when deployed;
# for local dev with `netlify dev`, copy to .env)
# Set these in Netlify: Site settings → Environment variables → Add variable

# --- Optional: single shared login when Hygraph is not used ---
DOCS_USERNAME=admin
DOCS_PASSWORD=your-secure-password

# --- Required: signs the session cookie ---
DOCS_SESSION_SECRET=generate-a-long-random-string-here

# --- Hygraph: user store (create/delete users in Hygraph) ---
# If set, login uses Hygraph; otherwise uses DOCS_USERNAME/DOCS_PASSWORD
HYGRAPH_ENDPOINT=https://api-xxx.region.hygraph.com/v2/your-project-id
HYGRAPH_TOKEN=your-hygraph-permanent-auth-token

# --- Protects create/delete/list docs-user APIs (Bearer token) ---
DOCS_ADMIN_SECRET=another-long-random-string-for-admin-api
